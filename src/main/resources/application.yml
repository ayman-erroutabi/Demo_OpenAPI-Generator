server:
  port: 8036
spring:
  liquibase:
    change-log: classpath:db/db.changelog-master.xml
  datasource:
    url: jdbc:mysql://localhost:3306/openapidemo
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none

